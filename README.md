# ROXID module

The ROXID modules is required to run the ROXID templates ROXID One, ROXID Two, ROXID Three and ROXID Four.

You can install the module by running
```bash
composer require seemannit/roxid
```
